# Requirement changes

Pending requirement batches live here as **one file per coherent change** (feature slice).

- Copy `docs/requirements/changes/_template.md` to `YYYY-MM-DD-short-title.md`.
- See `docs/requirements-change-process.md` for the full workflow.
- No pending changes when this folder contains only `_template.md` and this README.
