# Local RESTler fuzzing (Development seed user — see appsettings.Development.json InitialAdministratorOptions)
RESTLER_LOGIN_EMAIL=admin@example.local
RESTLER_LOGIN_PASSWORD=admin123

# Optional overrides (leave empty for auto-detect on the Compose network):
# RESTLER_API_BASE=http://backend:8080/api/v1
# RESTLER_OPENAPI_URL=http://backend:8080/swagger/v1/swagger.json
